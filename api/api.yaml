openapi: 3.0.3
info:
  title: Emote Tracker API
  description: The Rest API for the Emote Tracker
  version: 1.0.0
paths:
  /user/register:
    get:
      tags:
        - registration
      summary: Register a new de.jonas.emote.tracker.backend.user to track the emote usage
      operationId: registerNewUser
      parameters:
        - in: query
          name: userId
          required: true
          description: Twitch User ID
          schema:
            type: string
      responses:
        200:
          description: Successfully registered new de.jonas.emote.tracker.backend.user
          content:
            'application/json':
              schema:
                type: string
        304:
          description: Nothing was changed. User already registered
          content:
            'application/json':
              schema:
                type: string

  /user/unregister:
    get:
      tags:
        - registration
      operationId: unregisterUser
      parameters:
        - in: query
          name: userId
          required: true
          description: Twitch User ID
          schema:
            type: string
      responses:
        200:
          description: Successfully unregistered de.jonas.emote.tracker.backend.user
          content:
            'application/json':
              schema:
                type: string
        304:
          description: Nothing was changed. User already unregistered/ never registered
          content:
            'application/json':
              schema:
                type: string

  /user/overview:
    get:
      tags:
        - de.jonas.emote.tracker.backend.user
      operationId: getUserOverview
      parameters:
        - in: query
          name: userId
          schema:
            type: string
      responses:
        200:
          description: Successfully got de.jonas.emote.tracker.backend.user overview
          content:
            'application/json':
              schema:
                type: string

  /user/{userId}/emotes/count/top:
    get:
      tags:
        - de.jonas.emote.tracker.backend.user
      operationId: getTopEmoteCount
      parameters:
        - in: path
          name: userId
          required: true
          schema:
            type: string
        - in: query
          name: isTop
          required: true
          schema:
            type: boolean
      responses:
        200:
          description: Successfully retrieved top/bottom 5 emotes based on their usage count
          content:
            'application/json':
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Emote'

components:
  schemas:
    Emote:
      type: object
      properties:
        id:
          type: string
        count:
          type: integer
          format: int32
